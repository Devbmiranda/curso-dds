// serve para procurar um texto dentro de outro texto , mas te retona onde esta a ultima ocorrencia e faz a pesquisda de tgraz para frente
// str.lastindexof(searchValue[, fromindex])

// 'ricardo'.lastindexOf('r'); //4
// 'ricardo'.lastindexOf('a', 3); //3
// 'ricardo'.lastindexOf('a', 0); //-1
// 'ricardo'.lastindexOf('x'); //-1
// 'ricardo'.lastindexOf('x', -5); //-1 quando é negativo considera-se o zer

